[variables]
NODE_ENV = "DEVELOPMENT"
NIXPACKS_METADATA = "website"

[phases.setup]
nixPkgs = ["bun"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = []  # No build step needed - Bun runs TypeScript directly

[start]
cmd = "bun run server.ts"